console.log(2>1);
console.log(2==1);
console.log(3>=6);

//  output in boolean!!

console.log("2" > 1);
// compare the value of same data types..
//  this comparison is so confusing as it compares string with number..
// it doesn't gives predictable output
// "==" wroks with different syntax in case of comparison operator.

console.log("2"=== 2);
// "===" Strict comparison -- this checks the datatypes also  and avoid the conversion and gives the exact result.